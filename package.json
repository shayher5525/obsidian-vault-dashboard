{
	"name": "vault-dashboard-x",
	"version": "0.11.0",
	"description": "A vault dashboard with an activity heatmap, folder distribution, and configurable visual-folder quick links.",
	"license": "GPL-3.0-only",
	"private": true,
	"scripts": {
		"version": "node version-bump.mjs && git add manifest.json versions.json"
	}
}
